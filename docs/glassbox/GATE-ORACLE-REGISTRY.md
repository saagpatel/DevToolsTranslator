# Glassbox Gate Oracle Registry

Status: accepted for Gate 0 review
Owner: primary implementer and independent verifier

An oracle listed as `planned` is an assigned later-gate deliverable, not evidence that the control currently passes. Gate promotion requires the named executable path, a demonstrated failing fixture, a passing production case, and a versioned receipt bound to the changed artifact or fixture corpus. `scripts/glassbox/candidate_manifest.py` freezes one clean Git head/tree plus the exact core, Browser, OTLP, passive-context, and process-context release bytes; every promoted external lane and Gate 7 evidence must carry that same manifest digest.

| Gate | Oracle | Planned executable path | Required receipt/result | Current state |
|---|---|---|---|---|
| 0 | Contract completeness and prohibited-source matcher | `scripts/glassbox/check_gate0.py` | `glassbox-gate0-receipt/v2`; provisional precommit plus authoritative clean-commit/tree binding | Implemented; self-test, independent review, and commit-bound pass receipt required before promotion |
| 1 | Package/dependency direction | `scripts/glassbox/check_boundaries.py` | `glassbox-boundary-receipt/v1` | Implemented; enforces separate SQLCipher runtime workspace and no DTT edges |
| 1 | Bundle and dependency closure against component dispositions | `scripts/glassbox/check_boundaries.py` | Receipt lists every linked/bundled component and rejects `excluded` or unclassified legacy code | Rust and JavaScript package closure implemented; final bundle closure still blocks shell integration |
| 1 | Identity collision, semantic/materialization round trip, clock partial order | Rust tests in `glassbox-contracts`, `glassbox-kernel`, `glassbox-storage-sqlite`, and `glassbox-import-coordinator` | Cargo test evidence | Implemented; encrypted schema v2 separates canonical semantics from materializations, v1 backfill is transactional, complete bundle re-import preserves semantics and adds lineage/materialization records, and interrupted bundle streams publish nothing |
| 1 | Encryption/App Sandbox feasibility | `scripts/glassbox/verify_storage_sandbox_spike.sh` | `glassbox-storage-spike/v1` with candidate/license/WAL/crash/perf/signed-sandbox result | Implemented; passed locally on target arm64 Mac, exact clean-commit receipt required |
| 1 | Atomic migration/staging/crash recovery | Rust tests in `glassbox-storage-sqlite`, `glassbox-import-coordinator`, and worker integration tests | Forced process abort before commit proves no partial canonical publication; replay/collision/wrong-key/migration tests | Implemented; forced abort rolls back, schema-v1 materializations backfill transactionally, interrupted worker streams cannot publish, and complete metadata-only HAR, OTLP trace, and Glassbox-bundle streams publish atomically |
| 2 | Hostile import budgets/confinement | `scripts/glassbox/run_hostile_import_gate.sh` plus `scripts/glassbox/parser-fuzz` | `glassbox-hostile-import/v1` containing `glassbox-parser-fuzz/v1` | Core worker, fixture-NDJSON, metadata-only PCAP/PCAPNG, streaming metadata-only HAR 1.2, OTLP JSONL trace, fixed-member Glassbox Evidence Bundle, and terminal-bound Apple-log projection corpora plus 4,096 deterministic mutations per parser with zero panics implemented; binary OTLP, OTLP metrics/logs/profiles, ZIP, raw `.logarchive`, and Instruments `.trace` remain disabled pending raw-source corpora and adapter oracles |
| 2 | Apple raw-source adapter readiness | `scripts/glassbox/run_apple_import_readiness_gate.sh` plus `scripts/glassbox/apple_import_promotion.py` | `glassbox-apple-import-readiness/v1` in readiness or strict promoted state | Public `OSLogStore` and Instruments child modes, terminal-bound projection, bounded tree hashing/staging, same-binary distribution, sandbox/no-network checks, sanitized failures, and completed projection/HAR re-import through the ordinary Rust bridge/kernel path are implemented. Strict promotion requires valid `.logarchive` and `.trace` corpora, a CMS-signed independent corpus review chained to the supplied reviewer CA, deterministic metadata-only log projection, bounded signed `xctrace` conversion, Unknown/no-relation kernel imports, and zero source-path leakage; partial external configuration fails closed |
| 2 | Key lifecycle, plaintext residue, retention, crypto-shred | `scripts/glassbox/run_key_lifecycle_readiness.sh` then strict `scripts/glassbox/run_key_lifecycle_gate.sh` | `glassbox-key-lifecycle-readiness/v1` embeds the profile-backed `glassbox-key-lifecycle/v1` strict receipt when available | Crypto unit/clippy and Keychain-policy structure pass locally. Without a product-authorized provisioning profile the receipt remains non-promotable. With `GLASSBOX_KEY_PROVISIONING_PROFILE`, the same runner executes the signed restart/crypto-shred/cleanup oracle in a fresh isolated build target and promotes only an exact all-true strict receipt bound to the current Git tree |
| 2 | Seeded-secret/redaction/export derivation | `scripts/glassbox/run_privacy_gate.sh` | `glassbox-privacy-gate/v1` with zero leaks | Implemented; clean commit-bound receipt required for promotion |
| 3 | Five mystery families and epistemic restraint | `scripts/glassbox/run_mystery_acceptance.sh` | `glassbox-mystery-gate/v1` | Fixture/read-model oracle and interactive rendered workbench implemented; clean commit-bound receipt required |
| 3 | Native supported-file import workflow | `scripts/glassbox/run_native_import_workflow_gate.sh` | `glassbox-native-import-workflow/v1` | User-selected HAR, OTLP JSONL, PCAP/PCAPNG, and Glassbox bundles flow by inherited file descriptor through the isolated Rust helper, hostile parser, complete-stream assembler, evidence kernel, and bounded native projection; content-digest mismatch, seeded secrets, paths, time/output limits, minimal entitlements, preserve-on-failure, and 150 signed imported-workspace interactions are covered; raw `.logarchive` and `.trace` remain disabled |
| 3 | Keyboard, VoiceOver, zoom, reduced motion, tabular equivalence | `scripts/glassbox/run_accessibility_gate.sh` plus `scripts/glassbox/accessibility_evidence.py` | `glassbox-accessibility/v1` with optional exact `glassbox-accessibility-manual/v1` evidence | Native labeled-control, keyboard-command, export-sheet, complete-table, Swift test, and release-build checks pass locally. Manual promotion requires a fresh CA-verified independent CMS review bound to the frozen candidate, exact final app-binary and DMG hashes, eight all-true checks, and four unique typed confined attachment hashes; unsigned/stale evidence, unknown fields, non-booleans, duplicate paths, escapes, symlinks, and hash drift fail closed |
| 4 | Native Messaging selected-tab identity, privacy, frame, revoke, and lifecycle | `scripts/glassbox/run_browser_ipc_gate.sh` containing `scripts/glassbox/run_browser_adapter_gate.sh` | `glassbox-browser-ipc/v1` containing `glassbox-browser-adapter/v1` | Separately distributed hardened zero-entitlement native adapter plus Rust Native Messaging host and MV3 DevTools panel implemented. The public-key-derived extension has only `nativeMessaging` permission and no host permissions, content scripts, background worker, scripting, or tab API. Exact origin, visible inspected-tab consent, one-use challenge, short credential, monotonic frames, metadata-only strict payload, URL/request pseudonyms, explicit gaps/manual markers/stop, kernel validation, private atomic inbox, explicit non-overwrite export, offline core re-import, wrong-origin/replay/unknown/disconnect zero-bundle controls, manifest reset, and unchanged two-executable sandboxed core pass locally. Browser evidence remains `signed_untrusted`; manual markers are `user_asserted`. Published extension identity, fresh-VM Chrome end-to-end flow, notarization/stapling, and manual DevTools/native accessibility proof remain |
| 4 | Live-source auth, quotas, epoch/replay/gaps | `scripts/glassbox/run_live_source_gate.sh`, `scripts/glassbox/run_otlp_broker_gate.sh`, and `scripts/glassbox/run_otlp_adapter_lifecycle_gate.sh` | `glassbox-live-source/v1` containing `glassbox-otlp-broker/v1`, `glassbox-otlp-adapter-lifecycle/v1`, and `glassbox-reference-instrumented-workflow/v1` | Contract plus Developer-ID-signed hardened App Sandbox runtime pass on IPv4/IPv6 loopback; immutable absolute and session quotas, wrong-peer-then-valid survival, non-loopback rejection, partial-frame preservation, explicit stop/revocation gap, watchdog/disconnect/quota gaps, secret exclusion, and outbound denial pass. Complete live projections are kernel-validated, written only to an inherited descriptor, digest-bound, raw-content-free, and re-imported through the native kernel; poisoned input publishes zero bytes. The separately signed native adapter contains exactly a sandboxed SwiftUI controller and entitlement-free Rust helper. A separately signed sandboxed client-only reference instrumented source receives its credential only over stdin, rejects arguments and non-loopback configuration, drives the Swift controller workflow, produces a hash-bound receipt proving private content exclusion and atomic evidence publication, and re-imports that exact emitted bundle through the signed core Rust bridge. Local install/update/downgrade/revoke/uninstall/residue checks pass without mutating or entering the two-executable core bundle. Fresh-VM notarized lifecycle, an independently instrumented third-party app, and sustained production-load study remain |
| 4 | Bounded aggregate system resource session | `scripts/glassbox/run_resource_sampler_gate.sh` | `glassbox-resource-sampler/v1` | Native visible start/stop, 30-second default cap, aggregate load/memory allowlist, explicit terminal coverage, fail-closed bounds, source-declared trust, Unknown/no-relation projection, signed native render, exact minimal app entitlements, and entitlement-free isolated helper are implemented; process sampling remains excluded from this core path |
| 4 | Bounded selected-application process context | `scripts/glassbox/run_process_context_gate.sh` | `glassbox-process-context-gate/v1` containing `glassbox-process-adapter-workflow/v1` | Separately signed native adapter contains exactly a zero-entitlement SwiftUI controller and zero-entitlement Rust evidence helper. Explicit GUI-app selection, 30-second absolute bound, stop/cancel, PID-reuse detection, inherited-descriptor consent and publication, kernel validation, bundle digest, private field allowlist, no relations, Unknown core re-import, invalid-input zero-byte publication, lifecycle preservation, and absence from the two-executable sandboxed core pass locally. A signed sandbox negative control proves cross-process sampling is denied in the core posture and publishes zero bytes. Fresh-VM, notarization/stapling, and manual keyboard/VoiceOver/disclosure/identity proof remain |
| 4 | Default-deny egress and injected blocked socket | `scripts/glassbox/run_egress_gate.sh` plus `scripts/glassbox/run_network_runtime_gate.sh` | `glassbox-egress/v1` containing `glassbox-network-runtime/v1` | Core/worker/UI dependency and source denylist, signed sandbox-worker blocked-network oracle, separately accounted signed brokers, signed core-app entitlements, and process-scoped macOS socket observation across fixture/import/browse/compare/export workflows implemented; positive socket control is mandatory |
| 4 | Observer effect | `scripts/glassbox/run_observer_effect_gate.sh` | `glassbox-observer-effect/v1` | Bounded 1,000-frame broker probe implemented with RSS, elapsed-time, throughput, and secret-exclusion checks; the resource sampler has a finite functional gate but sustained sampler overhead and full UI workflow studies remain |
| 5 | PCAP fidelity, bounded streaming, opacity and no process attribution | `scripts/glassbox/run_network_import_gate.sh` | `glassbox-network-import/v1` | Streaming PCAP micro/nano and PCAPNG little/big-endian parser plus signed sandbox-worker path implemented; packet-native locators, timestamp resolution, explicit opacity/truncation, seeded-payload exclusion, malformed corpus, 20,000-packet RSS bound, and no-process-attribution oracle pass; broader link types remain explicitly opaque |
| 5 | Passive local-network logical context | `scripts/glassbox/run_passive_context_gate.sh` containing `scripts/glassbox/run_passive_adapter_lifecycle_gate.sh` | `glassbox-passive-context/v1` containing `glassbox-passive-adapter-lifecycle/v1` and `glassbox-passive-adapter-workflow/v1` | Separately signed native one-shot adapter implemented with a SwiftUI controller and entitlement-free Rust helper. The controller is App-Sandboxed with only user-selected-file access; neither executable has network or privileged entitlements. Visible disclosure, cancel, non-overwrite export, inherited-descriptor consent, fixed `/usr/sbin/arp -an`, bounded parser/timeout, conflict preservation, malformed/stale/active-scan rejection, and explicit untrusted/noncausal limitations are covered. Product evidence removes addresses, link-layer identifiers, and interfaces; preserves only state, conflict, count, and limitations; kernel-validates before inherited-descriptor publication; emits a digest-bound `GLSBX001` bundle; publishes zero bytes on malformed input; and re-imports through the signed core Rust bridge. Signed sandbox runtime and local install/update/downgrade/uninstall/user-export preservation pass while the unchanged core remains exactly two executables. Fresh-VM, notarization/stapling, and manual keyboard/VoiceOver/disclosure proof remain |
| 6 | Million-event performance | `scripts/glassbox/run_performance_gate.sh` plus signed interaction check in `scripts/glassbox/run_macos_artifact_readiness.sh` | `glassbox-performance/v1` and nested `glassbox-native-interaction/v1` | Exact encrypted million-event run passes with 1,000-record atomic batches, `synchronous=FULL`, 180.05-second ingest, 0.297 ms navigation p95, 92.8 MB peak RSS, ten explicit gaps, and zero unmarked drops. Canonical payloads are stored once and materializations use a backward-compatible versioned marker; cache and WAL checkpoint bounds are explicit. Sustained observer-effect and manual accessibility studies remain |
| 6 | Signing, entitlements, Gatekeeper, notarization, staple | `scripts/glassbox/verify_macos_artifact.sh` plus `scripts/glassbox/run_macos_artifact_readiness.sh` | `glassbox-macos-artifact/v1`, bound to `.app` and DMG hashes; readiness is not a pass receipt | Isolated Glassbox shell and local Developer-ID artifact readiness oracle implemented; strict receipt remains `ok: false` and non-promotable until exact app/DMG notarization, stapling, and Gatekeeper acceptance pass |
| 6 | Browser Adapter distribution, Chrome fresh-VM flow, accessibility, and residue | `scripts/glassbox/run_browser_artifact_readiness.sh` plus `scripts/glassbox/verify_browser_artifact.sh` | `glassbox-browser-artifact/v1`, bound to the frozen candidate, adapter executable, Rust host, DMG, extension ZIP, extension ID, and CMS-authenticated `glassbox-browser-fresh-vm/v1` evidence | Local Developer-ID, Hardened Runtime, zero-entitlement, exact executable closure, privacy manifest, six-file extension allowlist, exact extension identity/permission, DMG, and ZIP readiness pass. Strict promotion remains false until the exact artifacts are notarized/stapled, accepted by Gatekeeper, and matched to fresh CA-verified attachment-backed Chrome, accessibility, reset, and uninstall evidence |
| 6 | OTLP, passive-context, and process-context adapter distribution | `scripts/glassbox/run_auxiliary_adapter_artifact_readiness.sh` plus `scripts/glassbox/verify_auxiliary_adapters.py` | `glassbox-auxiliary-adapters/v1`, bound to the frozen candidate, all three app/binary/helper/DMG hashes, and CMS-authenticated `glassbox-auxiliary-adapters-fresh-vm/v1` evidence | Local Developer-ID, Hardened Runtime, exact per-adapter entitlement allowlists, two-executable closures, privacy manifests, signed DMGs, and filesystem checks are mandatory. Strict promotion remains false until every exact app and DMG is notarized/stapled, accepted by Gatekeeper, and matched to fresh CA-verified attachment-backed consent, capture, stop, revoke, reset, uninstall, residue, keyboard, VoiceOver, and disclosure evidence |
| 6 | Final-artifact privacy API inventory and manifest claims | `scripts/glassbox/privacy_artifact_audit.py` via `scripts/glassbox/run_macos_artifact_readiness.sh` | Nested `glassbox-privacy-artifact/v1`, bound to the signed Mach-O, privacy manifest, and fresh reviewed policy hashes | Final signed executable is inspected for all five current Apple required-reason API categories; exact reviewed symbol/string inventory, no-tracking/no-collection manifest claims, macOS applicability, and 90-day policy freshness fail closed on drift |
| 6 | Install/update/downgrade/revoke/uninstall/residue | `scripts/glassbox/run_lifecycle_gate.sh` plus `scripts/glassbox/lifecycle_evidence.py` | `glassbox-lifecycle/v1` with optional exact `glassbox-lifecycle-fresh-vm/v1` evidence | Local signed-artifact harness covers install, launch, permission denial, update preservation, downgrade rejection, revoke, reset, uninstall, reinstall, user-work preservation, and residue negative controls. `GLASSBOX_LIFECYCLE_USE_EXISTING_ARTIFACTS=1` binds that local receipt to the exact final candidate without rebuilding it. Promotion then requires a separate verify-only fresh-VM receipt bound to the matching local receipt, final app binary, Rust helper, and DMG plus fifteen exact lifecycle checks and at least five confined attachment hashes |
| 6 | Pilot, power analysis, preregistered human benchmark | `scripts/glassbox/validate_benchmark_results.py`, `scripts/glassbox/verify_benchmark_promotion.py`, and `scripts/glassbox/run_benchmark_readiness_gate.sh` | `glassbox-benchmark-readiness/v1` embedding a valid `glassbox-benchmark/v1` study receipt and CA-verified `glassbox-benchmark-independent-verification/v1` CMS payload for promotion | Public 60-scenario rehearsal corpus and fail-closed validator/readiness oracle pass locally. Promotion requires the complete held-out human-study artifact and separate independent CMS payload to bind the same frozen candidate plus distinct, path-confined, hash-matched attachments for participant registry, study records, scoring, adjudication, preregistration, held-out corpus, and metric recomputation. Partial inputs, bare booleans, unsigned/stale evidence, mismatched hashes or candidate identity, invalid study structure, signature failure, unknown fields, symlinks, duplicate files, path escapes, or non-boolean checks cannot promote |
| 7 | Donor parity/soak/rollback/approval | `scripts/glassbox/check_retirement_readiness.py` plus `scripts/glassbox/run_retirement_readiness_gate.sh` | `glassbox-retirement/v1` | Fail-closed evidence schema, validator, CMS authentication and raw-JSON negative controls, future-time rejection, and readiness receipt implemented for Codec, Pulse Orbit, and Echolocate. Strict promotion requires all three records together, each signed by the product authority and chained to the supplied CA, plus genuine parity, two-release, 30-day soak, defect, expert-workflow, final-release, rollback, and archive-preservation evidence |
| 0-7 | Consolidated program readiness | `scripts/glassbox/run_program_readiness.sh` plus `scripts/glassbox/program_readiness.py` | `glassbox-program-readiness/v1` with SHA-256 references to every nested gate receipt, frozen candidate manifest, and explicit `promotion_checks` | Full local matrix executes each gate family, requires exact receipt schemas, accepts either valid readiness or valid promoted states, computes `program_promotable`, `highest_promotable_gate`, and blocker text from receipt evidence, and currently remains non-promotable. It cannot promote dirty/uncommitted Gate 0 state, missing or inconsistent candidate identity, profile-less Keychain readiness, disabled Apple raw imports, unstapled core, browser, OTLP, passive-context, or process-context artifacts, manual-accessibility readiness, an absent independently verified human benchmark, incomplete fresh-VM lifecycle evidence, or missing retirement evidence |

## Failure policy

Missing executable, missing failing control fixture, missing receipt, unknown result, or stale artifact binding is `NO-GO`. A prose claim cannot substitute for an oracle result.
