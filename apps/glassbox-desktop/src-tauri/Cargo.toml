[package]
name = "glassbox-desktop-shell"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
tauri = { version = "2.11", default-features = false, features = ["wry"] }

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[workspace]
